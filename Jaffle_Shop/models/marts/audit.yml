version: 2

models:
  - name: audit_deleted_customers_history
    tests:
      - audit_delete_counts:
          raw_table: source('ecom', 'raw_customers')   # ✅ use source or ref correctly
          key_column: id
      

